# Ultra-minimal CI requirements for basic functionality testing
pytest>=8.2.0
pytest-asyncio>=0.23.7

# Essential for basic framework functionality
pydantic>=2.8.0
structlog>=24.2.0

# Note: All other dependencies are optional for basic testing
# Ray, gRPC, FastAPI, etc. will be mocked or skipped if not available
