# Ultra-minimal CI requirements for basic functionality testing
pytest>=8.2.0
pytest-asyncio>=0.23.7

# Code quality tools (lightweight versions)
ruff>=0.5.0
mypy>=1.10.0

# Essential for basic framework functionality
pydantic>=2.8.0
structlog>=24.2.0

# Note: Ray, gRPC, FastAPI, etc. are optional - tests will use local fallbacks
